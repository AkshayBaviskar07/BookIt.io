server:
  port: 8083
spring:
  application:
    name: ticket-service
  datasource:
    url: jdbc:h2:mem:ticketdb
    username: sa
    password: password
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
